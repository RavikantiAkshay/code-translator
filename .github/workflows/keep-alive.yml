name: Backend Keep-Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Runs every 3 minutes
  workflow_dispatch:        # Allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Endpoint
        run: |
          curl -f https://code-translator-h5rv.onrender.com/health
